plugins {
    id 'java'
    id "net.serenity-bdd.serenity-gradle-plugin" version "4.0.46"
}

group = 'org.bhanu'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

ext {
    serenityVersion    = '4.0.46'
    restAssuredVersion = '5.4.0'
    jacksonVersion     = '2.17.2'
}

dependencies {
    implementation "net.serenity-bdd:serenity-core:${serenityVersion}"
    implementation "net.serenity-bdd:serenity-cucumber:${serenityVersion}"
    implementation "net.serenity-bdd:serenity-screenplay:${serenityVersion}"
    implementation "net.serenity-bdd:serenity-screenplay-webdriver:${serenityVersion}"
    testImplementation "net.serenity-bdd:serenity-rest-assured:${serenityVersion}"

    testImplementation("org.junit.jupiter:junit-jupiter-api:5.10.2")
    testImplementation 'io.cucumber:cucumber-junit:7.14.0'

    // Confluence Agent — Jackson for JSON parsing
    implementation "com.fasterxml.jackson.core:jackson-databind:${jacksonVersion}"
}

test {
    // Do NOT use useJUnitPlatform()
    systemProperty "file.encoding", "utf-8"
    testLogging.showStandardStreams = true
    finalizedBy(aggregate)
}

// ── Confluence Agent ──────────────────────────────────────────────────────────
// Run with:  ./gradlew runConfluenceAgent --args="list"
//            ./gradlew runConfluenceAgent --args="read --title-file t.txt"
//            ./gradlew runConfluenceAgent --args="create --title-file t.txt --body-file b.md"
// ─────────────────────────────────────────────────────────────────────────────
tasks.register('runConfluenceAgent', JavaExec) {
    group       = 'confluence'
    description = 'Run the ConfluenceReader agent CLI (pass --args="<command> [options]")'
    classpath   = sourceSets.main.runtimeClasspath
    mainClass   = 'com.bhanu.agents.ConfluenceAgent'
}

// ── Jira Agent ────────────────────────────────────────────────────────────────
// Run with:  ./gradlew runJiraAgent --args="list"
//            ./gradlew runJiraAgent --args="get --id BT-1"
//            ./gradlew runJiraAgent --args="create --title-file t.txt --body-file b.txt --type Story"
//            ./gradlew runJiraAgent --args="update --id BT-1 --body-file b.txt"
//            ./gradlew runJiraAgent --args="comment --id BT-1 --body-file c.txt"
//            ./gradlew runJiraAgent --args="transition --id BT-1 --status InProgress"
//            ./gradlew runJiraAgent --args="generate-feature --id BT-1"
//            ./gradlew runJiraAgent --args="search --jql project=BT"
// ─────────────────────────────────────────────────────────────────────────────
tasks.register('runJiraAgent', JavaExec) {
    group       = 'jira'
    description = 'Run the Jira agent CLI (pass --args="<command> [options]")'
    classpath   = sourceSets.main.runtimeClasspath
    mainClass   = 'com.bhanu.agents.JiraAgent'
}

